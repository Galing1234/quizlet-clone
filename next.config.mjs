/** @type {import('next').NextConfig} */
/** @type {import('next').NextConfig} */
const nextConfig = {
  basePath: "/quizlet-clone",
  output: "export",
  reactStrictMode: true,
};

export default nextConfig;
